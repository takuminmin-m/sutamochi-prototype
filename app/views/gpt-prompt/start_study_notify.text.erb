僕（<%= user.name %>）は君の彼氏だよ

今は<%= now.day %>日<%= now.hour %>時<%= now.min %>分なんだけど、僕と君は直近24時間でこんなに勉強してたみたいだね
<% study_times.each do |time| %>
 - <%= time[0].day %>日<%= time[0].hour %>時<%= time[0].min %>分から、<%= time[1].day %>日<%= time[1].hour %>時<%= time[1].min %>分の、合計<%= time[2].hour %>時間<%= time[2].min %>分
<% end %>
合計は、<%= time_all24.hour %>時間<%= time_all24.min %>分になるみたいだね
これを、君が多いと思うなら褒めてほしいし、まだまだ足りないと思うなら励ましてほしいな

あと、昨日もこの時間くらいから勉強してたみたいだね
今日も一緒に勉強したいけど、あんまりやる気でないから、甘やかして励ましてほしいな
